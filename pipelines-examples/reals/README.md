# This are somekind of real and working examples. 

* gitpipe : a pipeline: first we fetch the github repo, and we run some test.


## USEFULL COMDS:

Execute the job directly on your machine and get the results live.
```console
fly -t lite execute -c hello.yml 
```
